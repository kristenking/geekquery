<div id="app"></div>
<div class="d-flex my-3 justify-content-center gap-5">
  <div class="flex flex-column">
    <% current_user.questions.each do |p| %>
      <%= render 'questions/question', question: p %>
    <% end %> 
</div> 
</div>
